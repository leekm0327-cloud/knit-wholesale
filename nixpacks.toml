[phases.setup]
nixPkgs = ["nodejs_22", "npm-10_x"]

[phases.install]
cmds = ["npm install --no-audit --no-fund --prefer-offline"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node dist/index.cjs"
