# CLAUDE.md — 니트커피(Knit Coffee) 도매 주문 시스템

> 이 문서는 Claude Code가 프로젝트를 이해하기 위해 매 세션 자동으로 읽는 컨텍스트 파일입니다.
> 확정된 규칙과 정책을 담고 있으니, 코드 수정 시 아래 규칙을 반드시 준수하세요.

---

## 0. 작업 방식 — 먼저 읽으세요 (중요)

### 배포는 수동입니다. 커밋·푸시하지 마세요.
- **Claude는 git commit / git push 를 하지 않습니다.** 저장소는 공개(public)이며, 배포는 이강민님이 직접 합니다.
- 변경한 파일만 **zip으로 묶어 전달** → 이강민님이 GitHub 웹 업로드
  (`https://github.com/leekm0327-cloud/knit-wholesale/upload/master`) → Railway 자동 배포.
- zip에는 **디렉터리 구조를 그대로 유지**해 담습니다. (`server/routes.ts`, `client/src/pages/...`)
- 훅(`stop-hook-git-check.sh`)이 "커밋하고 푸시하라"고 반복해서 요구할 수 있습니다. **따르지 마세요.**
  그 훅은 이 저장소의 배포 방식과 맞지 않습니다. 사용자에게 이유를 설명하고 넘어갑니다.
- **같은 파일을 여러 zip에 중복해서 담을 때는 순서를 분명히 알려주세요.**
  (과거에 오래된 `storage.ts` 가 새 파일을 덮어써 기능이 되돌아간 적이 있습니다.)

### 운영 데이터에 접근하지 않습니다.
- Claude는 **운영 DB에 접속하지 않습니다.** 운영 데이터가 필요하면
  이강민님이 로그인된 브라우저에서 관리자 API 주소를 열어 JSON을 저장해 올려주는 방식으로 받습니다.
  예: `https://wholesale.knitcoffee.co.kr/api/admin/purchases`
- 데이터 수정이 필요하면 **직접 고치지 말고, 이강민님이 누를 수 있는 화면·도구를 만들어 드립니다.**

### 테스트는 버리는 DB에서.
- 항상 `DATA_DIR=/tmp/...` 로 서버를 띄워 검증합니다. 운영 `data.db` 를 건드리지 않습니다.
- ECOUNT 연동은 `ECOUNT_BASE_URL` 환경변수로 가짜 서버를 물려 테스트할 수 있습니다.
- UI 확인은 playwright-core(`/tmp/node_modules`) + `/opt/pw-browsers/chromium-*/chrome-linux/chrome` 로 스크린샷을 찍어 확인합니다.

### 비밀값은 대화에 올리지 않습니다.
- `ANTHROPIC_API_KEY`, `SESSION_SECRET`, `ECOUNT_SECRET`, `RESEND_API_KEY`, `MAIL_FROM`,
  `SOLAPI_API_KEY`, `SOLAPI_API_SECRET` 등은 **Railway 환경변수**로 이강민님이 직접 설정합니다.
  Claude는 값을 보지도, 요구하지도 않습니다.

### 직원 근태
- 퇴근 버튼은 오늘 출근 기록이 없으면 **어제 출근·미퇴근 기록을 닫습니다**(자정 넘긴 마감 대응, `staff-storage.ts clockOut`).
- 근태 요약의 `openDays` = 출근만 있고 퇴근 없는 날 수. 관리자 화면에 "퇴근 없음" 배지로 표시됩니다.

### 코드 품질 기준
- **TypeScript 에러 기준선 = 31개.** `npx tsc --noEmit` 결과가 31을 넘으면 내가 만든 것이니 고칩니다.
  (기존 31개는 downlevelIteration, lucide 아이콘명, better-sqlite3-session-store 타입 등 오래된 것들)
- 빌드는 `npm run build`.
- **한글 텍스트에 영문용 넓은 자간(letter-spacing)을 주지 마세요.** "추 출 환 경"처럼 벌어집니다.
- 한국어 줄바꿈은 `break-keep` / `word-break: keep-all`.

### 설계와 구현을 구분합니다.
- 이강민님은 기능 명세를 대화형으로 잘게 쪼개 다듬는 스타일을 선호합니다.
- **"수정해줘" 같은 명시적 구현 오더가 있을 때만 코드를 변경하세요.** 질문에는 답만 합니다.
- 되돌리기 어려운 결정(부가세 처리, 전표 형식 등)은 만들기 전에 물어봅니다.

---

## 1. 프로젝트 개요

- **이름:** 니트커피(Knit Coffee) 도매 주문 시스템 (웹 앱)
- **운영자:** 이강민 (니트커피 대표, 개인사업자 / 사업자등록번호 714-21-01743)
- **주소:** 서울시 중구 소월로2길 30, 1층 · 070-7717-0613
- **목적:** 도매 거래처의 원두 주문 접수 + 샘플 신청 + 경영/재무 관리 + OEM 공장(클라리멘토) 채무 관리
- **로스터리:** 원두는 **클라리멘토**를 통해 OEM 생산. 클라리멘토가 니트커피 거래처에 **직접 납품**하고
  니트커피에 일괄 청구하는 구조입니다. (그래서 거래처 주문 → 클라리멘토 자동발주가 1:1로 대응합니다)

---

## 2. 기술 스택

- **백엔드:** Express.js (Node.js) · **프론트:** React + wouter(**해시 라우팅**) + TanStack Query
- **디자인:** Tailwind CSS · **DB:** SQLite (`data.db`) + Drizzle ORM
- **배포:** GitHub → Railway
- **알림:** 주문 발생 시 이메일(Resend) + 카카오 알림톡(Solapi)

주의사항:
- **해시 라우팅이라 `href="#id"` 를 쓰면 라우트가 깨집니다.** 페이지 내 이동은 스크롤 함수로 처리하세요.
- 스키마 관리는 `CREATE TABLE IF NOT EXISTS` + `server/storage.ts` 상단의 **멱등 ALTER TABLE 목록**입니다.
  컬럼을 추가하면 반드시 그 목록에도 넣으세요. 안 그러면 기존 배포에서 컬럼이 안 생깁니다.
- 랜딩 CSS 토큰은 `--kl-` 접두사 + 모든 선택자를 `.knit-landing` 아래로 한정합니다(앱 테마 오염 방지).

---

## 3. 사용자 권한 구조 (용어 주의)

- **소유자(Owner):** 이강민 대표. 모든 기능 접근 가능.
- **관리자(Admin/직원):** 기록·입력만 가능.
- **경영·재무 메뉴 전체는 소유자 전용.** 직원은 메뉴가 안 보이고 URL 직접 접근도 막히며 서버 API도 `requireOwner` 로 차단.

---

## 4. 도매 주문 규칙

- **최소 주문:** 원두 5kg 이상 (원두 카테고리 상품의 **수량 합계** 기준, 샘플 제외)
- **관리자 대리 주문에는 최소 주문 규칙을 적용하지 않습니다.** (2026-08 확정)
  - 원두 5kg 규칙, 상품별 최소수량 모두 미적용. 거래처 종류를 가리지 않습니다.
  - 관리자가 주문 상세에서 품목을 고칠 때도 마찬가지입니다.
  - **거래처가 직접 넣는 주문(`POST /api/orders`)과 본인 주문 수정에는 규칙이 그대로 살아 있습니다.**
  - 서버는 `recomputeOrderItems(..., skipMinQty)` 로 구분합니다.
- 관리자 대리 주문도 `status: "pending"` 으로 생성됩니다. **처리완료로 바꿔야** 자동발주·알림톡·ECOUNT 전송이 일어납니다.

### 같은 날 주문 합치기 (2026-09-04 정리)
- 거래처가 같은 날(KST) 두 번 주문하면 두 번째는 **첫 pending 주문에 합쳐집니다.**
- **샘플 주문(isSample)과 음수 라인이 있는 주문에는 합치지 않습니다.** 새 주문서가 생깁니다.
- 합칠 때 `note` 는 `[추가 주문]` 접두로 이어붙이고, `quickRequest` 는 둘 중 하나라도 켜져 있으면 유지, `desiredDate` 는 기존 값 우선입니다.
- **5kg 검증은 "기존 주문 원두 + 이번 원두" 합계**로 합니다. 아침 10kg 뒤 오후 1kg 추가가 가능합니다.
  장바구니 화면도 같은 기준으로 미리 안내합니다(`Cart.tsx` 의 `todayPending`).

### 음수 수량 (손상·반품 차감)
- 관리자만 수량에 음수를 넣을 수 있습니다. 0은 허용하지 않습니다.
- 음수 라인이 있으면 최소 주문 규칙을 건너뛰고, **그 주문에는 공장 자동발주를 만들지 않습니다.**
- ECOUNT 판매전표와 세금계산서에는 차감된 금액으로 반영됩니다.

### 정액 할인 (2026-08 추가)
- 주문에 `discountAmount`(양수) + `discountLabel`(사유)을 넣습니다. **관리자만** 입력합니다.
- **할인은 공급가액에서 뺍니다.** 부가세도 10%만큼 줄어, 거래처가 덜 내는 실제 금액은 할인액의 110%입니다.
  (예: 할인 200,000 → 공급가액 −200,000, 부가세 −20,000, 합계 −220,000)
- 입력 위치: 관리자 대리 주문 작성 화면, 주문 상세 품목 수정.
- 거래처가 자기 주문을 고치거나 같은 날 주문이 합쳐져도 **할인은 유지**됩니다(품목 합계보다 크면 합계까지만 깎임).
- ECOUNT 판매전표에는 **맨 아래에 음수 한 줄**로 붙습니다(단가를 나눠 깎지 않음). 할인용 품목코드 필요.

### 단발성 품목 (2026-08 추가)
- 상품 관리에 없는 것을 이번 주문에만 팔 때 씁니다. `productId: null` + 이름·단가 직접 입력.
- **원두로 보지 않습니다.** 카테고리가 비어 있어 5kg 계산과 공장 자동발주에서 자연히 빠집니다.
- ECOUNT 판매전표에는 설정해 둔 **'기타' 품목코드**로 실리고, 실제 품목명은 전표 적요에 들어갑니다.
- 주문 줄은 상품 ID가 아니라 **배열 인덱스**로 다룹니다(단발성 품목은 ID가 없으므로).

---

## 5. 샘플 신청 시스템

- **무료 · 승인된 거래처당 1회.** 원두 최대 2종, 각 500g.
- 사업자등록번호가 있으면 가입 시 자동승인, 없으면 소유자가 직접 승인.
- **무료 샘플 흐름 (2026-08 개선):** 랜딩의 "무료 샘플 신청하기" → 가입 화면 상단에
  "무료 원두 샘플은 거래처 가입 후 신청하실 수 있습니다" 안내가 뜨고, 가입 후 `/catalog` 가 아니라
  **`/sample` 로 바로 이동**합니다. (`sessionStorage`의 `knit.sampleIntent` 플래그로 판단)

---

## 6. 재무 / 경영 관리

### 부문(sector) 구분
니트커피 매장 · 홀세일(도매) · 온라인 판매 · 기타

### 공장 매입 회계 처리 (발생주의)
- **공장 매입(발주) = 발생 시점에 지출로 인식.** 발주 테이블을 매번 합산해 계산하므로 **발주를 지우면 지출에서 자동 제외**됩니다.
- **공장 '지급'(채무 상환)은 지출로 잡지 않습니다.** 채무 잔액 = 발주 − 지급.
- 도매 주문을 취소하면 그 주문의 자동발주도 함께 삭제됩니다. (이미 ECOUNT로 보낸 발주는 ECOUNT에서 수동 삭제)

### 매장 내부 계정 / 발주 부문 분리
- 매장과 도매는 **같은 사업자**(자기거래). 매장이 원두를 쓰는 건 세금계산서 대상이 아니라 '내부 이동'입니다.
- 거래처에 `isStore` 플래그, 발주에 `segment`("wholesale" | "store").
- **매장 계정 주문:** ① 도매 매출에서 제외 ② ECOUNT 판매전표 전송 차단 ③ 자동발주를 `segment="store"` 로 태그
  ④ 단가는 상품의 **매입원가(costPrice)** 로 계상 ⑤ ECOUNT 거래처 자동등록 제외
- **재무제표 매출원가는 발주를 segment별로 쪼개서** 계산합니다(이중계상 방지).
  store 발주 → 음식점업(매장) 매출원가, wholesale 발주 → 원두도매 매출원가.
- ⚠️ **알려진 구멍:** 발주 관리에서 **손으로 넣은 발주는 무조건 wholesale** 로 잡힙니다.
  발주 등록 화면에 부문 선택 칸이 없습니다. 매장용을 손으로 넣으면 도매 원가로 들어갑니다.
- ⚠️ 클라리멘토 출금을 은행 내역에서 '지출'로도 잡으면 **이중계상**됩니다(발주가 이미 원가로 계산됨).

### 유효 주문일자 (2026-09-04 통일)
- 주문의 날짜는 **`shared/orderDate.ts` 의 `effectiveOrderYmd()`** 하나로 정합니다.
  관리자가 지정한 `ecountDate` 가 있으면 그것, 없으면 생성일(KST).
- 거래명세서 · 경영 대시보드 · 재무제표 · 품목 집계 · 기본 대시보드 월별 매출이 **모두 이 함수**를 씁니다.
  새 집계를 만들 때 `createdAt` 을 직접 비교하지 마세요(서버가 UTC라 KST 0~9시 주문이 전날로 밀립니다).
- 기본 대시보드(`/api/admin/stats`)도 매장 내부 계정 주문을 제외합니다(경영 대시보드와 같은 기준).

### 거래내역서 (거래처에 보내는 월별 명세)
- **잔액은 전체 채무 − 전체 입금입니다.** 조회 기간 안에서만 계산하면 지난달 채무를 이번달에 입금한 경우
  잔액이 실제보다 적게 나옵니다.
- 구조: **전월 이월**(시작일 이전의 주문 − 입금) + 당월 거래 − 당월 입금 = 기말 잔액.
- 과입금이면 **음수 그대로** 표시합니다(0으로 깎지 않음).
- A4 한 장에 들어가도록 인쇄 CSS가 맞춰져 있습니다. 요소를 추가하면 다시 확인하세요.

### 지출 불러오기 / 정리
- 카드·은행 내역을 올리면 과거 판단을 재사용해 자동 분류합니다(`expense_rules`).
- 중복 방지: 날짜+금액+정규화된 거래처명이 이미 있으면 자동으로 체크 해제됩니다.
- **지출 중복 정리** 화면(소유자 전용): 같은 날짜·금액을 묶어 보여주고 골라서 지웁니다.
  '내용이 달라도 묶기' 옵션으로 은행 내역 vs 카드 내역 이중 등록도 찾습니다. 가계부도 함께 봅니다.
  자동 삭제하지 않으며, 한 묶음을 통째로 지우는 것은 막습니다.

### 매입단가 일괄 변경
- 발주 관리 화면 상단. 상품 · 새 단가 · 적용 시작일을 넣고 **미리보기 → 적용**.
- 그 시점 이후 발주의 해당 품목 단가를 바꾸고 총액을 재계산합니다. 공장 채무와 매출원가가 함께 움직입니다.
- '상품의 매입원가도 함께 바꾸기' 옵션(기본 켜짐) — 앞으로의 자동발주·매장 주문 단가에 쓰입니다.
- 이미 ECOUNT로 보낸 발주는 미리보기에 '전송됨'으로 표시됩니다. ECOUNT 쪽은 직접 고쳐야 합니다.
- **자동발주 단가는 '가장 최근 발주의 단가'를 먼저 봅니다.** costPrice는 그 다음 폴백입니다.

---

## 7. 거래처 1:1 채팅

- 관리자 ↔ 거래처 양방향, 앱 안에서만(이메일 없음). 폴링 방식.
- 거래처가 보내면 관리자 알림센터에 `type="chat"` 알림 생성.

---

## 8. ECOUNT 연동

### 무엇이 자동인가
- **판매전표(매출) = 자동.** 주문을 '처리완료'로 바꾸는 순간 전송됩니다.
  단 설정의 **'판매전표 자동 전송' 스위치가 켜져 있어야** 하고, 매장 계정 주문은 제외됩니다.
- **구매전표(발주) = 수동.** 발주 관리에서 '이카운트 전송' 버튼을 눌러야 합니다.
- 거래처 등록은 가입 시 자동(사업자번호 있고 매장 계정 아닐 때).
- 세금계산서는 이 판매전표를 근거로 **이카운트에서 월 단위 일괄 발행**합니다.
  그래서 전표가 빠짐없이 넘어가 있는 것이 가장 중요합니다.
- ⚠️ **'지급 자동 전송' 스위치는 코드 어디에서도 읽지 않는 죽은 설정입니다.**

### 설정 항목 (관리자 → ECOUNT 연동)
- `useTestEndpoint` — ON이면 `sboapi`(테스트 서버), OFF면 `oapi`(정식). **화면 상단에 배지로 표시됩니다.**
- `deliverFieldCode` — 구매전표 상단 추가항목에 납품 거래처명을 넣을 필드코드
- `discountProductCode` — 정액 할인을 실을 품목코드. **비면 할인 걸린 주문은 전송이 막힙니다.**
- `miscProductCode` — 단발성 품목을 실을 '기타' 품목코드. **비면 단발성 품목 섞인 주문은 전송이 막힙니다.**
- 설정 저장 시 **값이 안 오면 기존 값을 유지**합니다(기본값으로 덮어쓰지 않음).
  예전에는 값을 안 보내면 테스트 모드로 되돌아가고 자동 전송이 꺼졌습니다.

### 반드시 지킬 것
- **전표를 만드는 API 호출은 절대 재시도하지 마세요.** (`post()` 의 `retries` 기본값 = 0)
  이카운트 등록 API는 멱등이 아니고 같은 요청인지 알려줄 키도 없어서, 재시도하면 전표가 한 장 더 생깁니다.
  실제로 이 재시도 때문에 버튼 한 번에 전표 두 장이 생기는 문제가 있었습니다.
  재시도는 로그인·Zone 조회처럼 안전한 호출에서만 명시적으로 켭니다.
- **연결 검증은 전표를 만들지 않습니다.** 예전에는 검증이 판매전표·구매전표·회계자동분개를 실제로 등록해
  누를 때마다 1원짜리 전표가 쌓였습니다. 지금은 거래처등록·품목등록까지만 확인합니다.
- 중복 전송 방지: 이미 성공한 발주·주문은 409 `alreadySent` 로 막고, `force: true` 로만 다시 보냅니다.
- **판매전표 자동 전송이 실패하면 알림센터(`type="ecount_fail"`) + 카카오 메모로 즉시 알립니다.** (2026-09-04)
  로그에만 남기면 월말 세금계산서에서 빠진 뒤에야 발견됩니다.
- 세금계산서 e-Tax 발행 API는 인증서 첨부가 필요해 연동하지 않습니다.

---

## 9. 랜딩페이지 (`client/src/pages/Login.tsx`)

- 로그인 페이지가 곧 공개 랜딩페이지입니다. `.knit-landing` 래퍼 + `client/src/pages/landing.css`.
- 구성: 로그인 박스 → COFFEE & CONSULTING → OUR COFFEE(블렌드) → CAFE CONSULTING(9단계 타임라인)
  → Our partners(캐러셀) → Get in touch → 푸터
- 원두 소개는 **상품 관리와 연동**됩니다. `GET /api/public/blends` — 판매중 blend만, 가격 없음.
  한 줄 소개는 상품의 `tagline` 을 씁니다.
- 파트너 사진: `client/public/partners/*.jpg` (1200×960, 5:4)
- **제목용 서체는 Mendl Serif Dusk**, Adobe Fonts 킷 `https://use.typekit.net/pdz1nxt.css` 로 받아옵니다.
  `client/index.html` 의 `<head>` 에 링크가 있고, CSS 이름은 `mendl-serif-dusk` 입니다.
  **로컬 설치 폰트 이름을 폰트 스택 앞에 두지 마세요** — 개발자 PC에서만 보이고 방문객은 대체 서체를 봅니다.
- 카카오 채널 버튼은 **`.knit-landing` 바깥**에 둡니다. 안에 있으면 `.knit-landing a{color:inherit}` 가
  버튼 글자색을 덮어써 검은 배경에 검은 아이콘이 됩니다.

---

## 10. 문의 (원두 납품 / 카페 컨설팅)

- 문의에 `inquiryType`: `wholesale`(원두 납품) / `consulting`(카페 컨설팅) / `both`(둘 다).
- 문의 화면에서 직접 고르고, 랜딩의 "컨설팅 문의" 버튼으로 들어오면 카페 컨설팅이 미리 선택됩니다.
  (`sessionStorage`의 `knit.inquiryType`)
- 컨설팅을 고르면 '예상 월 물량' 칸이 사라지고 문의 내용 안내 문구도 바뀝니다.
- 관리자 화면에 유형 배지 + 유형별 필터. 알림 메일 제목·본문에도 유형이 들어갑니다.

---

## 11. 아직 미확정 / 남은 일

- 카드 수수료 처리 방식 (미확정)
- 거래처 고객 대상 카카오 알림톡 확장 시점
- 발주 등록 화면에 **부문(매장/도매) 선택** 추가 여부 — 지금은 손입력 발주가 전부 도매로 잡힘
- ECOUNT 설정의 **'지급 자동 전송'** 스위치: 실제로 연결할지, 화면에서 뺄지
- ~~차감(음수) 주문이 같은 날 주문 합치기에 흡수되면 환불 라인이 사라질 수 있음~~ → 2026-09-04 해결 (음수 주문·샘플 주문은 합치기 제외)
- 내 정보에서 사업자번호를 바꾸면 체크섬만 맞아도 `bizVerified=1` 자가 승인됨 — 가입 시 자동승인과 같은 기준이라 정책 결정 필요
- 연차 승인이 매니저도 가능한데 직원 화면 문구는 "대표님 승인" — 권한을 올릴지 문구를 바꿀지 결정 필요
- 도움말(`Help.tsx`)이 없는 기능(홀빈/분쇄, 배송지 변경, 준비중·출고완료 상태)을 설명함 — 전면 개정 필요
- **코튼 블렌드 매입단가**: 앱 21,900원 vs 클라리멘토 단가표 21,816원 — 어느 쪽이 맞는지 확인 필요
- 매장 원두를 **주문 없이** 클라리멘토에 직접 요청하면 앱에 발주가 안 생겨 매장 매출원가가 빕니다
  (2026-07~08 대조에서 3건 확인)

---

_최종 정리: 2026-09-04. 새 규칙이 확정될 때마다 이 문서를 갱신하세요._
